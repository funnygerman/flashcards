{
  "name": "flashcards",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Flashcards in the browser: no build step, no dependencies, no accounts",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "node scripts/dev-server.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.10"
  }
}
