# Superseded documents

Historical only. Nothing here is authoritative.

- `requirements.md` — the original combined requirements, before the library/application split. Kept because
  it records the project's original intent, including several statements later overridden by decisions in
  [`../decisions/`](../decisions/): per-card ES modules loaded at runtime, the opt-in personal dictionary,
  up/down gestures as future work, and the library setting `100dvh` itself.

For the current specifications see [`../library/requirements.md`](../library/requirements.md) and
[`../app/requirements.md`](../app/requirements.md).
